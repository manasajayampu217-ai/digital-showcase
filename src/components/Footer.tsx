const Footer = () => {
  return (
    <footer className="py-8 px-4 border-t border-border text-center">
      <p className="text-xs text-muted-foreground font-body">
        © {new Date().getFullYear()} Jayampu Manasa. Built with passion.
      </p>
    </footer>
  );
};

export default Footer;
